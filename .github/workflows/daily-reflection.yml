on:
  schedule:
  #- cron: "*/5 * * * *"
  - cron: "44 4 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Make and push changes
      run: |
        git config --global user.email "williamthazard@protonmail.com"
        git config --global user.name "williamthazard"
        git commit -m "daily update"
        git push -u origin main
